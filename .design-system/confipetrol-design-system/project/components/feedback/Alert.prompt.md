**Alert** — inline message banner with left tone bar + icon (Font Awesome).

```jsx
<Alert tone="success" title="Servicio adjudicado">Codelco Salvador — minería.</Alert>
<Alert tone="warning" onClose={dismiss}>Documentación pendiente de cargar.</Alert>
```

Tones: `info` `success` `warning` `danger`.
